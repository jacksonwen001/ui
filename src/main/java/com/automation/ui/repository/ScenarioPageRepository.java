package com.automation.ui.repository;

import com.automation.ui.models.ScenarioPage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScenarioPageRepository extends JpaRepository<ScenarioPage, Long> {
}
